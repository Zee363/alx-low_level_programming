#include <stdio.h>

/**
 *Description:main - A program that prints out a line using puts function
 *
 *Return: Always 0 (Success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
