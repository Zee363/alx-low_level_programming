#include "main.h"
#include <stdio.h>

/**
 * mul - A function that multilpies two integers
 * @e: The first integer
 * @f: The second integer
 * Return: Always 0
 */

int mul(int e, int f)
{
	int result = e * f;

	return (result);
}
