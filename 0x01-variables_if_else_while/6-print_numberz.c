#include <stdio.h>

/**
 * main - A program that prints out all single digits of base 10
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int e;

	e = 0;
	while
		(e < 10) {
			putchar(e + '0');
			e++;
		}
	putchar('\n');
	return (0);
}
