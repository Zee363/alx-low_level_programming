#include "main.h"

/**
 * _islower - checks gor lowercase characters
 * @c: character to be checked
 * Return: 1 if c is lowercase, 0 otherwise
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
